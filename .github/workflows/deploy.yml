name: CI/CD for Spring Boot Backend (Gradle)

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Lấy code từ repo
      - name: Checkout code
        uses: actions/checkout@v3

      # 2️⃣ Cài đặt JDK 22
      - name: Set up JDK 22
        uses: actions/setup-java@v3
        with:
          java-version: '22'
          distribution: 'temurin'

      # 3️⃣ Cấp quyền cho Gradle Wrapper
      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      # 4️⃣ Build Spring Boot app bằng Gradle
      - name: Build Spring Boot app
        run: ./gradlew clean build -x test

      # 5️⃣ Đăng nhập Docker Hub
      - name: Log in to Docker Hub
        run: |
          echo ${{ secrets.DOCKER_PASSWORD }} | docker login -u ${{ secrets.DOCKER_USERNAME }} --password-stdin

      # 6️⃣ Build Docker image và đẩy lên Docker Hub
      - name: Build & Push Docker image
        run: |
          IMAGE_NAME=${{ secrets.DOCKER_USERNAME }}/fwfe-backend:latest
          docker build -t $IMAGE_NAME .
          docker push $IMAGE_NAME

      # 7️⃣ SSH vào VPS để pull image mới và khởi động container
      - name: Deploy to VPS using password
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.VPS_HOST }}
          username: root
          password: ${{ secrets.VPS_PASS }}
          script: |
            echo "==== 🚀 Triển khai backend lên VPS ===="
            docker pull ${{ secrets.DOCKER_USERNAME }}/fwfe-backend:latest

            # Dừng container cũ nếu đang chạy
            docker stop fwfe-backend || true
            docker rm fwfe-backend || true

            # Chạy container mới
            docker run -d \
              --name fwfe-backend \
              -p 8080:8080 \
              -e SPRING_DATASOURCE_URL=${{ secrets.DB_URL }} \
              -e SPRING_DATASOURCE_USERNAME=${{ secrets.DB_USERNAME }} \
              -e SPRING_DATASOURCE_PASSWORD=${{ secrets.DB_PASSWORD }} \
              -e JWT_SECRET=${{ secrets.JWT_SECRET }} \
              ${{ secrets.DOCKER_USERNAME }}/fwfe-backend:latest

            echo "✅ Deploy thành công! Ứng dụng đang chạy trên cổng 8080."
